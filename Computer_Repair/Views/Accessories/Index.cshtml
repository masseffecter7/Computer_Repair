@model IEnumerable<Computer_Repair.Models.Accessorie>

@{
    ViewBag.Title = "Accessories";
}

<h2>Accessories</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.KindsOfAccessories.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.KindsOfAccessories.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AccessorieId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AccessorieId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AccessorieId })
        </td>
    </tr>
}

</table>
